# Deployment credentials

Files in this directory contain deployment credentials needed to reproduce or recover the bridge configuration. They are intentionally tracked so credentials transfer with this private repository.

The repository must remain private. Do not copy these files into technician-facing configuration exports, public repositories, issue descriptions, or logs.
